验证两个对象相等。。。 address or value

test3.lir 中的循环怎么处理呢

bb2store: HashMap<&lir::Block, store::ConstantStore>

pub struct ControlFlowGraph {
    // A DAG representing the control flow of a program
    nodes: HashMap<u32, lir::Block>,
    edges: Vec<(u32, u32)>,
}

pub struct ConstantStore<'a> {
    status: HashMap<&'a lir::Variable, domain::Constant>,
}


topo orders of blocks: {"bb3": 3, "dummy_entry": 0, "bb1": 2, "bb2": 5, "entry": 1, "dummy_exit": 4}
topo orders of blocks: {"entry": 1, "bb2": 5, "bb1": 2, "dummy_exit": 4, "bb3": 3, "dummy_entry": 0}
